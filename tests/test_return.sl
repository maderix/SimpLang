/* Test proper return handling */
fn main() {
    var result = 0.0;
    
    // Test some calculations
    result = 10.0 + 5.0;  // Should be 15.0
    
    // Return the result
    return result;  // Should properly terminate the function
}
